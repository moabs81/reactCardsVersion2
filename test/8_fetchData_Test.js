import chai from 'chai';

import fetchData from '../src/components/locationCard/jsModules/fetchData';

describe('xHrRequest', () => {
    it('is not being tested right now because I have no idea how to test it', () => {
    });
});